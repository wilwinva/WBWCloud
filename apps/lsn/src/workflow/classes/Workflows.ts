interface Workflow12 {
  radio1: {
    checked: boolean;
    checkboxes: { check1: boolean; check2: boolean; check3: boolean; check4: boolean; check5: boolean };
  };
  radio2: { checked: boolean };
  result3: { checkboxes: { check6: boolean; check7: boolean; check8: boolean; check9: boolean } };
}
