import React from 'react';

test.skip('When rendering UniversityData component then DtnList should be rendered correctly', async () => {
  /**
   const wrapper = mount(
   <MockedProvider mocks={basicDtnListTestMocks(DtnModCategories.uccsn, TransferComponent.SPA)} addTypename={true}>
   <MemoryRouter timeout={0}>
   <UniversityData />
   </MemoryRouter>
   </MockedProvider>
   );
   await basicDtnListTest(wrapper);
   */
});
