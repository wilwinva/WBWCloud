import React from 'react';

test.skip('When rendering CountyData component then DtnList should render correctly', async () => {
  /**
   const wrapper = mount(
   <MockedProvider mocks={basicDtnListTestMocks(DtnModCategories.nye, TransferComponent.SPA)} addTypename={true}>
   <MemoryRouter timeout={0}>
   <CountyData />
   </MemoryRouter>
   </MockedProvider>
   );
   await basicDtnListTest(wrapper);
   */
});
